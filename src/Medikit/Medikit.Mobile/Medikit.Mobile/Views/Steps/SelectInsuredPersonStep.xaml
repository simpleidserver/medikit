<?xml version="1.0" encoding="UTF-8"?>
<controls:StepBarProgressContentControl xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Medikit.Mobile.Controls"
             Title="Select insured person"
             mc:Ignorable="d"
             xmlns:resources="clr-namespace:Medikit.Mobile.Resources"
             x:Class="Medikit.Mobile.Views.Steps.SelectInsuredPersonStep">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackLayout Grid.Row="0">
            <Label>Select insured person</Label>
        </StackLayout>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="2" Command="{Binding NextCommand}" Text="{x:Static resources:AppResources.Next}"></Button>
        </Grid>
    </Grid>
</controls:StepBarProgressContentControl>